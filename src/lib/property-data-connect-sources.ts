/** Planned integrations — UI only; no live API calls. */
export const CONNECTABLE_PROPERTY_DATA_SOURCES = [
  "City assessor records",
  "County parcel / GIS records",
  "Zoning API / zoning map",
  "Building permits",
  "Tax assessment records",
  "Recorded sales / deed records",
  "OpenStreetMap / geocoding",
  "Manual underwriting inputs",
] as const;
